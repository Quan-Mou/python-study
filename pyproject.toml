[project]
name = "python-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "QuanMou", email = "2312038309@qq.com" }
]
requires-python = ">=3.10"
dependencies = [
    "lxml>=6.1.3",
    "mysql-connector-python>=26.7.0",
    "openai>=3.13.0",
    "pandas>=2.3.3",
    "requests>=2.34.2",
]

[project.scripts]
python-study = "python_study:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
